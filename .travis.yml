language: java
jdk:
  - oraclejdk7
before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
after_failure:
  - "cat ${TRAVIS_BUILD_DIR}/logs/zeppelin*.log"
  - "cat ${TRAVIS_BUILD_DIR}/logs/zeppelin*.out"
